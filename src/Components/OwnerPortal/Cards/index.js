import "./style.css";

//email = user's email
//user = type of user; Either "owner" or "renter"
//type = type of fetch; Either "rent", "applications", or "maintenance"
const Cards = ({ email, type }) => (
    //TODO: add in a <ul> element for each result of the given fetch
    <span />
)

export default Cards;
